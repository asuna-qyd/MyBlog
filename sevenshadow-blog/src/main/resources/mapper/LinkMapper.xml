<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asuna.mapper.LinkMapper">

    <select id="getAllLink" resultType="com.asuna.vo.LinkVo">
        select * from sg_link where status = 1 and del_flag = 0
    </select>
</mapper>
